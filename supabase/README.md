# Supabase

Supabase-related resources live here.

Examples for later:

- SQL migrations
- local development config
- seed scripts

Current status:

- initial `users` and `workspaces` schema migration
- first-login trigger for automatic personal workspace creation
