steps:
  - name: gcr.io/cloud-builders/docker
    env:
      - DOCKER_BUILDKIT=1
    args:
      - build
      - -f
      - apps/web/Dockerfile
      - -t
      - ${_IMAGE_URI}
      - .

images:
  - ${_IMAGE_URI}
